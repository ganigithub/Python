# reverse method: .reverse(): reverses the elements of list

numbers = [1,2,3,4,5]
numbers.reverse()
print(numbers)

print(numbers.reverse()) #this returns None. so we shouldn't assign this to a variable